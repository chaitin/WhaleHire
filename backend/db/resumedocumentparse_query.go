// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/WhaleHire/backend/db/predicate"
	"github.com/chaitin/WhaleHire/backend/db/resume"
	"github.com/chaitin/WhaleHire/backend/db/resumedocumentparse"
	"github.com/google/uuid"
)

// ResumeDocumentParseQuery is the builder for querying ResumeDocumentParse entities.
type ResumeDocumentParseQuery struct {
	config
	ctx        *QueryContext
	order      []resumedocumentparse.OrderOption
	inters     []Interceptor
	predicates []predicate.ResumeDocumentParse
	withResume *ResumeQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResumeDocumentParseQuery builder.
func (rdpq *ResumeDocumentParseQuery) Where(ps ...predicate.ResumeDocumentParse) *ResumeDocumentParseQuery {
	rdpq.predicates = append(rdpq.predicates, ps...)
	return rdpq
}

// Limit the number of records to be returned by this query.
func (rdpq *ResumeDocumentParseQuery) Limit(limit int) *ResumeDocumentParseQuery {
	rdpq.ctx.Limit = &limit
	return rdpq
}

// Offset to start from.
func (rdpq *ResumeDocumentParseQuery) Offset(offset int) *ResumeDocumentParseQuery {
	rdpq.ctx.Offset = &offset
	return rdpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rdpq *ResumeDocumentParseQuery) Unique(unique bool) *ResumeDocumentParseQuery {
	rdpq.ctx.Unique = &unique
	return rdpq
}

// Order specifies how the records should be ordered.
func (rdpq *ResumeDocumentParseQuery) Order(o ...resumedocumentparse.OrderOption) *ResumeDocumentParseQuery {
	rdpq.order = append(rdpq.order, o...)
	return rdpq
}

// QueryResume chains the current query on the "resume" edge.
func (rdpq *ResumeDocumentParseQuery) QueryResume() *ResumeQuery {
	query := (&ResumeClient{config: rdpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rdpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rdpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumedocumentparse.Table, resumedocumentparse.FieldID, selector),
			sqlgraph.To(resume.Table, resume.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resumedocumentparse.ResumeTable, resumedocumentparse.ResumeColumn),
		)
		fromU = sqlgraph.SetNeighbors(rdpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResumeDocumentParse entity from the query.
// Returns a *NotFoundError when no ResumeDocumentParse was found.
func (rdpq *ResumeDocumentParseQuery) First(ctx context.Context) (*ResumeDocumentParse, error) {
	nodes, err := rdpq.Limit(1).All(setContextOp(ctx, rdpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resumedocumentparse.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) FirstX(ctx context.Context) *ResumeDocumentParse {
	node, err := rdpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResumeDocumentParse ID from the query.
// Returns a *NotFoundError when no ResumeDocumentParse ID was found.
func (rdpq *ResumeDocumentParseQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rdpq.Limit(1).IDs(setContextOp(ctx, rdpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resumedocumentparse.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rdpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResumeDocumentParse entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResumeDocumentParse entity is found.
// Returns a *NotFoundError when no ResumeDocumentParse entities are found.
func (rdpq *ResumeDocumentParseQuery) Only(ctx context.Context) (*ResumeDocumentParse, error) {
	nodes, err := rdpq.Limit(2).All(setContextOp(ctx, rdpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resumedocumentparse.Label}
	default:
		return nil, &NotSingularError{resumedocumentparse.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) OnlyX(ctx context.Context) *ResumeDocumentParse {
	node, err := rdpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResumeDocumentParse ID in the query.
// Returns a *NotSingularError when more than one ResumeDocumentParse ID is found.
// Returns a *NotFoundError when no entities are found.
func (rdpq *ResumeDocumentParseQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rdpq.Limit(2).IDs(setContextOp(ctx, rdpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resumedocumentparse.Label}
	default:
		err = &NotSingularError{resumedocumentparse.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rdpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResumeDocumentParses.
func (rdpq *ResumeDocumentParseQuery) All(ctx context.Context) ([]*ResumeDocumentParse, error) {
	ctx = setContextOp(ctx, rdpq.ctx, ent.OpQueryAll)
	if err := rdpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResumeDocumentParse, *ResumeDocumentParseQuery]()
	return withInterceptors[[]*ResumeDocumentParse](ctx, rdpq, qr, rdpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) AllX(ctx context.Context) []*ResumeDocumentParse {
	nodes, err := rdpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResumeDocumentParse IDs.
func (rdpq *ResumeDocumentParseQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if rdpq.ctx.Unique == nil && rdpq.path != nil {
		rdpq.Unique(true)
	}
	ctx = setContextOp(ctx, rdpq.ctx, ent.OpQueryIDs)
	if err = rdpq.Select(resumedocumentparse.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rdpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rdpq *ResumeDocumentParseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rdpq.ctx, ent.OpQueryCount)
	if err := rdpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rdpq, querierCount[*ResumeDocumentParseQuery](), rdpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) CountX(ctx context.Context) int {
	count, err := rdpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rdpq *ResumeDocumentParseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rdpq.ctx, ent.OpQueryExist)
	switch _, err := rdpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rdpq *ResumeDocumentParseQuery) ExistX(ctx context.Context) bool {
	exist, err := rdpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResumeDocumentParseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rdpq *ResumeDocumentParseQuery) Clone() *ResumeDocumentParseQuery {
	if rdpq == nil {
		return nil
	}
	return &ResumeDocumentParseQuery{
		config:     rdpq.config,
		ctx:        rdpq.ctx.Clone(),
		order:      append([]resumedocumentparse.OrderOption{}, rdpq.order...),
		inters:     append([]Interceptor{}, rdpq.inters...),
		predicates: append([]predicate.ResumeDocumentParse{}, rdpq.predicates...),
		withResume: rdpq.withResume.Clone(),
		// clone intermediate query.
		sql:       rdpq.sql.Clone(),
		path:      rdpq.path,
		modifiers: append([]func(*sql.Selector){}, rdpq.modifiers...),
	}
}

// WithResume tells the query-builder to eager-load the nodes that are connected to
// the "resume" edge. The optional arguments are used to configure the query builder of the edge.
func (rdpq *ResumeDocumentParseQuery) WithResume(opts ...func(*ResumeQuery)) *ResumeDocumentParseQuery {
	query := (&ResumeClient{config: rdpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rdpq.withResume = query
	return rdpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResumeDocumentParse.Query().
//		GroupBy(resumedocumentparse.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (rdpq *ResumeDocumentParseQuery) GroupBy(field string, fields ...string) *ResumeDocumentParseGroupBy {
	rdpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResumeDocumentParseGroupBy{build: rdpq}
	grbuild.flds = &rdpq.ctx.Fields
	grbuild.label = resumedocumentparse.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.ResumeDocumentParse.Query().
//		Select(resumedocumentparse.FieldDeletedAt).
//		Scan(ctx, &v)
func (rdpq *ResumeDocumentParseQuery) Select(fields ...string) *ResumeDocumentParseSelect {
	rdpq.ctx.Fields = append(rdpq.ctx.Fields, fields...)
	sbuild := &ResumeDocumentParseSelect{ResumeDocumentParseQuery: rdpq}
	sbuild.label = resumedocumentparse.Label
	sbuild.flds, sbuild.scan = &rdpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResumeDocumentParseSelect configured with the given aggregations.
func (rdpq *ResumeDocumentParseQuery) Aggregate(fns ...AggregateFunc) *ResumeDocumentParseSelect {
	return rdpq.Select().Aggregate(fns...)
}

func (rdpq *ResumeDocumentParseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rdpq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rdpq); err != nil {
				return err
			}
		}
	}
	for _, f := range rdpq.ctx.Fields {
		if !resumedocumentparse.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if rdpq.path != nil {
		prev, err := rdpq.path(ctx)
		if err != nil {
			return err
		}
		rdpq.sql = prev
	}
	return nil
}

func (rdpq *ResumeDocumentParseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResumeDocumentParse, error) {
	var (
		nodes       = []*ResumeDocumentParse{}
		_spec       = rdpq.querySpec()
		loadedTypes = [1]bool{
			rdpq.withResume != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResumeDocumentParse).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResumeDocumentParse{config: rdpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rdpq.modifiers) > 0 {
		_spec.Modifiers = rdpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rdpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rdpq.withResume; query != nil {
		if err := rdpq.loadResume(ctx, query, nodes, nil,
			func(n *ResumeDocumentParse, e *Resume) { n.Edges.Resume = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rdpq *ResumeDocumentParseQuery) loadResume(ctx context.Context, query *ResumeQuery, nodes []*ResumeDocumentParse, init func(*ResumeDocumentParse), assign func(*ResumeDocumentParse, *Resume)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResumeDocumentParse)
	for i := range nodes {
		fk := nodes[i].ResumeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(resume.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "resume_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rdpq *ResumeDocumentParseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rdpq.querySpec()
	if len(rdpq.modifiers) > 0 {
		_spec.Modifiers = rdpq.modifiers
	}
	_spec.Node.Columns = rdpq.ctx.Fields
	if len(rdpq.ctx.Fields) > 0 {
		_spec.Unique = rdpq.ctx.Unique != nil && *rdpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rdpq.driver, _spec)
}

func (rdpq *ResumeDocumentParseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resumedocumentparse.Table, resumedocumentparse.Columns, sqlgraph.NewFieldSpec(resumedocumentparse.FieldID, field.TypeUUID))
	_spec.From = rdpq.sql
	if unique := rdpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rdpq.path != nil {
		_spec.Unique = true
	}
	if fields := rdpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resumedocumentparse.FieldID)
		for i := range fields {
			if fields[i] != resumedocumentparse.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rdpq.withResume != nil {
			_spec.Node.AddColumnOnce(resumedocumentparse.FieldResumeID)
		}
	}
	if ps := rdpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rdpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rdpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rdpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rdpq *ResumeDocumentParseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rdpq.driver.Dialect())
	t1 := builder.Table(resumedocumentparse.Table)
	columns := rdpq.ctx.Fields
	if len(columns) == 0 {
		columns = resumedocumentparse.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rdpq.sql != nil {
		selector = rdpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rdpq.ctx.Unique != nil && *rdpq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rdpq.modifiers {
		m(selector)
	}
	for _, p := range rdpq.predicates {
		p(selector)
	}
	for _, p := range rdpq.order {
		p(selector)
	}
	if offset := rdpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rdpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (rdpq *ResumeDocumentParseQuery) ForUpdate(opts ...sql.LockOption) *ResumeDocumentParseQuery {
	if rdpq.driver.Dialect() == dialect.Postgres {
		rdpq.Unique(false)
	}
	rdpq.modifiers = append(rdpq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return rdpq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (rdpq *ResumeDocumentParseQuery) ForShare(opts ...sql.LockOption) *ResumeDocumentParseQuery {
	if rdpq.driver.Dialect() == dialect.Postgres {
		rdpq.Unique(false)
	}
	rdpq.modifiers = append(rdpq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return rdpq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rdpq *ResumeDocumentParseQuery) Modify(modifiers ...func(s *sql.Selector)) *ResumeDocumentParseSelect {
	rdpq.modifiers = append(rdpq.modifiers, modifiers...)
	return rdpq.Select()
}

// ResumeDocumentParseGroupBy is the group-by builder for ResumeDocumentParse entities.
type ResumeDocumentParseGroupBy struct {
	selector
	build *ResumeDocumentParseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rdpgb *ResumeDocumentParseGroupBy) Aggregate(fns ...AggregateFunc) *ResumeDocumentParseGroupBy {
	rdpgb.fns = append(rdpgb.fns, fns...)
	return rdpgb
}

// Scan applies the selector query and scans the result into the given value.
func (rdpgb *ResumeDocumentParseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rdpgb.build.ctx, ent.OpQueryGroupBy)
	if err := rdpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeDocumentParseQuery, *ResumeDocumentParseGroupBy](ctx, rdpgb.build, rdpgb, rdpgb.build.inters, v)
}

func (rdpgb *ResumeDocumentParseGroupBy) sqlScan(ctx context.Context, root *ResumeDocumentParseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rdpgb.fns))
	for _, fn := range rdpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rdpgb.flds)+len(rdpgb.fns))
		for _, f := range *rdpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rdpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rdpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResumeDocumentParseSelect is the builder for selecting fields of ResumeDocumentParse entities.
type ResumeDocumentParseSelect struct {
	*ResumeDocumentParseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rdps *ResumeDocumentParseSelect) Aggregate(fns ...AggregateFunc) *ResumeDocumentParseSelect {
	rdps.fns = append(rdps.fns, fns...)
	return rdps
}

// Scan applies the selector query and scans the result into the given value.
func (rdps *ResumeDocumentParseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rdps.ctx, ent.OpQuerySelect)
	if err := rdps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeDocumentParseQuery, *ResumeDocumentParseSelect](ctx, rdps.ResumeDocumentParseQuery, rdps, rdps.inters, v)
}

func (rdps *ResumeDocumentParseSelect) sqlScan(ctx context.Context, root *ResumeDocumentParseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rdps.fns))
	for _, fn := range rdps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rdps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rdps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rdps *ResumeDocumentParseSelect) Modify(modifiers ...func(s *sql.Selector)) *ResumeDocumentParseSelect {
	rdps.modifiers = append(rdps.modifiers, modifiers...)
	return rdps
}
