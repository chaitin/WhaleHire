// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/WhaleHire/backend/db/jobposition"
	"github.com/chaitin/WhaleHire/backend/db/predicate"
	"github.com/chaitin/WhaleHire/backend/db/resume"
	"github.com/chaitin/WhaleHire/backend/db/resumejobapplication"
	"github.com/google/uuid"
)

// ResumeJobApplicationQuery is the builder for querying ResumeJobApplication entities.
type ResumeJobApplicationQuery struct {
	config
	ctx             *QueryContext
	order           []resumejobapplication.OrderOption
	inters          []Interceptor
	predicates      []predicate.ResumeJobApplication
	withResume      *ResumeQuery
	withJobPosition *JobPositionQuery
	modifiers       []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResumeJobApplicationQuery builder.
func (rjaq *ResumeJobApplicationQuery) Where(ps ...predicate.ResumeJobApplication) *ResumeJobApplicationQuery {
	rjaq.predicates = append(rjaq.predicates, ps...)
	return rjaq
}

// Limit the number of records to be returned by this query.
func (rjaq *ResumeJobApplicationQuery) Limit(limit int) *ResumeJobApplicationQuery {
	rjaq.ctx.Limit = &limit
	return rjaq
}

// Offset to start from.
func (rjaq *ResumeJobApplicationQuery) Offset(offset int) *ResumeJobApplicationQuery {
	rjaq.ctx.Offset = &offset
	return rjaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rjaq *ResumeJobApplicationQuery) Unique(unique bool) *ResumeJobApplicationQuery {
	rjaq.ctx.Unique = &unique
	return rjaq
}

// Order specifies how the records should be ordered.
func (rjaq *ResumeJobApplicationQuery) Order(o ...resumejobapplication.OrderOption) *ResumeJobApplicationQuery {
	rjaq.order = append(rjaq.order, o...)
	return rjaq
}

// QueryResume chains the current query on the "resume" edge.
func (rjaq *ResumeJobApplicationQuery) QueryResume() *ResumeQuery {
	query := (&ResumeClient{config: rjaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rjaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rjaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumejobapplication.Table, resumejobapplication.FieldID, selector),
			sqlgraph.To(resume.Table, resume.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resumejobapplication.ResumeTable, resumejobapplication.ResumeColumn),
		)
		fromU = sqlgraph.SetNeighbors(rjaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryJobPosition chains the current query on the "job_position" edge.
func (rjaq *ResumeJobApplicationQuery) QueryJobPosition() *JobPositionQuery {
	query := (&JobPositionClient{config: rjaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rjaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rjaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumejobapplication.Table, resumejobapplication.FieldID, selector),
			sqlgraph.To(jobposition.Table, jobposition.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resumejobapplication.JobPositionTable, resumejobapplication.JobPositionColumn),
		)
		fromU = sqlgraph.SetNeighbors(rjaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResumeJobApplication entity from the query.
// Returns a *NotFoundError when no ResumeJobApplication was found.
func (rjaq *ResumeJobApplicationQuery) First(ctx context.Context) (*ResumeJobApplication, error) {
	nodes, err := rjaq.Limit(1).All(setContextOp(ctx, rjaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resumejobapplication.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) FirstX(ctx context.Context) *ResumeJobApplication {
	node, err := rjaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResumeJobApplication ID from the query.
// Returns a *NotFoundError when no ResumeJobApplication ID was found.
func (rjaq *ResumeJobApplicationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rjaq.Limit(1).IDs(setContextOp(ctx, rjaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resumejobapplication.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rjaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResumeJobApplication entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResumeJobApplication entity is found.
// Returns a *NotFoundError when no ResumeJobApplication entities are found.
func (rjaq *ResumeJobApplicationQuery) Only(ctx context.Context) (*ResumeJobApplication, error) {
	nodes, err := rjaq.Limit(2).All(setContextOp(ctx, rjaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resumejobapplication.Label}
	default:
		return nil, &NotSingularError{resumejobapplication.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) OnlyX(ctx context.Context) *ResumeJobApplication {
	node, err := rjaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResumeJobApplication ID in the query.
// Returns a *NotSingularError when more than one ResumeJobApplication ID is found.
// Returns a *NotFoundError when no entities are found.
func (rjaq *ResumeJobApplicationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rjaq.Limit(2).IDs(setContextOp(ctx, rjaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resumejobapplication.Label}
	default:
		err = &NotSingularError{resumejobapplication.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rjaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResumeJobApplications.
func (rjaq *ResumeJobApplicationQuery) All(ctx context.Context) ([]*ResumeJobApplication, error) {
	ctx = setContextOp(ctx, rjaq.ctx, ent.OpQueryAll)
	if err := rjaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResumeJobApplication, *ResumeJobApplicationQuery]()
	return withInterceptors[[]*ResumeJobApplication](ctx, rjaq, qr, rjaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) AllX(ctx context.Context) []*ResumeJobApplication {
	nodes, err := rjaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResumeJobApplication IDs.
func (rjaq *ResumeJobApplicationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if rjaq.ctx.Unique == nil && rjaq.path != nil {
		rjaq.Unique(true)
	}
	ctx = setContextOp(ctx, rjaq.ctx, ent.OpQueryIDs)
	if err = rjaq.Select(resumejobapplication.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rjaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rjaq *ResumeJobApplicationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rjaq.ctx, ent.OpQueryCount)
	if err := rjaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rjaq, querierCount[*ResumeJobApplicationQuery](), rjaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) CountX(ctx context.Context) int {
	count, err := rjaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rjaq *ResumeJobApplicationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rjaq.ctx, ent.OpQueryExist)
	switch _, err := rjaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rjaq *ResumeJobApplicationQuery) ExistX(ctx context.Context) bool {
	exist, err := rjaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResumeJobApplicationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rjaq *ResumeJobApplicationQuery) Clone() *ResumeJobApplicationQuery {
	if rjaq == nil {
		return nil
	}
	return &ResumeJobApplicationQuery{
		config:          rjaq.config,
		ctx:             rjaq.ctx.Clone(),
		order:           append([]resumejobapplication.OrderOption{}, rjaq.order...),
		inters:          append([]Interceptor{}, rjaq.inters...),
		predicates:      append([]predicate.ResumeJobApplication{}, rjaq.predicates...),
		withResume:      rjaq.withResume.Clone(),
		withJobPosition: rjaq.withJobPosition.Clone(),
		// clone intermediate query.
		sql:       rjaq.sql.Clone(),
		path:      rjaq.path,
		modifiers: append([]func(*sql.Selector){}, rjaq.modifiers...),
	}
}

// WithResume tells the query-builder to eager-load the nodes that are connected to
// the "resume" edge. The optional arguments are used to configure the query builder of the edge.
func (rjaq *ResumeJobApplicationQuery) WithResume(opts ...func(*ResumeQuery)) *ResumeJobApplicationQuery {
	query := (&ResumeClient{config: rjaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rjaq.withResume = query
	return rjaq
}

// WithJobPosition tells the query-builder to eager-load the nodes that are connected to
// the "job_position" edge. The optional arguments are used to configure the query builder of the edge.
func (rjaq *ResumeJobApplicationQuery) WithJobPosition(opts ...func(*JobPositionQuery)) *ResumeJobApplicationQuery {
	query := (&JobPositionClient{config: rjaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rjaq.withJobPosition = query
	return rjaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResumeJobApplication.Query().
//		GroupBy(resumejobapplication.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (rjaq *ResumeJobApplicationQuery) GroupBy(field string, fields ...string) *ResumeJobApplicationGroupBy {
	rjaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResumeJobApplicationGroupBy{build: rjaq}
	grbuild.flds = &rjaq.ctx.Fields
	grbuild.label = resumejobapplication.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.ResumeJobApplication.Query().
//		Select(resumejobapplication.FieldDeletedAt).
//		Scan(ctx, &v)
func (rjaq *ResumeJobApplicationQuery) Select(fields ...string) *ResumeJobApplicationSelect {
	rjaq.ctx.Fields = append(rjaq.ctx.Fields, fields...)
	sbuild := &ResumeJobApplicationSelect{ResumeJobApplicationQuery: rjaq}
	sbuild.label = resumejobapplication.Label
	sbuild.flds, sbuild.scan = &rjaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResumeJobApplicationSelect configured with the given aggregations.
func (rjaq *ResumeJobApplicationQuery) Aggregate(fns ...AggregateFunc) *ResumeJobApplicationSelect {
	return rjaq.Select().Aggregate(fns...)
}

func (rjaq *ResumeJobApplicationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rjaq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rjaq); err != nil {
				return err
			}
		}
	}
	for _, f := range rjaq.ctx.Fields {
		if !resumejobapplication.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if rjaq.path != nil {
		prev, err := rjaq.path(ctx)
		if err != nil {
			return err
		}
		rjaq.sql = prev
	}
	return nil
}

func (rjaq *ResumeJobApplicationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResumeJobApplication, error) {
	var (
		nodes       = []*ResumeJobApplication{}
		_spec       = rjaq.querySpec()
		loadedTypes = [2]bool{
			rjaq.withResume != nil,
			rjaq.withJobPosition != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResumeJobApplication).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResumeJobApplication{config: rjaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rjaq.modifiers) > 0 {
		_spec.Modifiers = rjaq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rjaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rjaq.withResume; query != nil {
		if err := rjaq.loadResume(ctx, query, nodes, nil,
			func(n *ResumeJobApplication, e *Resume) { n.Edges.Resume = e }); err != nil {
			return nil, err
		}
	}
	if query := rjaq.withJobPosition; query != nil {
		if err := rjaq.loadJobPosition(ctx, query, nodes, nil,
			func(n *ResumeJobApplication, e *JobPosition) { n.Edges.JobPosition = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rjaq *ResumeJobApplicationQuery) loadResume(ctx context.Context, query *ResumeQuery, nodes []*ResumeJobApplication, init func(*ResumeJobApplication), assign func(*ResumeJobApplication, *Resume)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResumeJobApplication)
	for i := range nodes {
		fk := nodes[i].ResumeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(resume.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "resume_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rjaq *ResumeJobApplicationQuery) loadJobPosition(ctx context.Context, query *JobPositionQuery, nodes []*ResumeJobApplication, init func(*ResumeJobApplication), assign func(*ResumeJobApplication, *JobPosition)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResumeJobApplication)
	for i := range nodes {
		fk := nodes[i].JobPositionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jobposition.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "job_position_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (rjaq *ResumeJobApplicationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rjaq.querySpec()
	if len(rjaq.modifiers) > 0 {
		_spec.Modifiers = rjaq.modifiers
	}
	_spec.Node.Columns = rjaq.ctx.Fields
	if len(rjaq.ctx.Fields) > 0 {
		_spec.Unique = rjaq.ctx.Unique != nil && *rjaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rjaq.driver, _spec)
}

func (rjaq *ResumeJobApplicationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resumejobapplication.Table, resumejobapplication.Columns, sqlgraph.NewFieldSpec(resumejobapplication.FieldID, field.TypeUUID))
	_spec.From = rjaq.sql
	if unique := rjaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rjaq.path != nil {
		_spec.Unique = true
	}
	if fields := rjaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resumejobapplication.FieldID)
		for i := range fields {
			if fields[i] != resumejobapplication.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rjaq.withResume != nil {
			_spec.Node.AddColumnOnce(resumejobapplication.FieldResumeID)
		}
		if rjaq.withJobPosition != nil {
			_spec.Node.AddColumnOnce(resumejobapplication.FieldJobPositionID)
		}
	}
	if ps := rjaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rjaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rjaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rjaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rjaq *ResumeJobApplicationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rjaq.driver.Dialect())
	t1 := builder.Table(resumejobapplication.Table)
	columns := rjaq.ctx.Fields
	if len(columns) == 0 {
		columns = resumejobapplication.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rjaq.sql != nil {
		selector = rjaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rjaq.ctx.Unique != nil && *rjaq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rjaq.modifiers {
		m(selector)
	}
	for _, p := range rjaq.predicates {
		p(selector)
	}
	for _, p := range rjaq.order {
		p(selector)
	}
	if offset := rjaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rjaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (rjaq *ResumeJobApplicationQuery) ForUpdate(opts ...sql.LockOption) *ResumeJobApplicationQuery {
	if rjaq.driver.Dialect() == dialect.Postgres {
		rjaq.Unique(false)
	}
	rjaq.modifiers = append(rjaq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return rjaq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (rjaq *ResumeJobApplicationQuery) ForShare(opts ...sql.LockOption) *ResumeJobApplicationQuery {
	if rjaq.driver.Dialect() == dialect.Postgres {
		rjaq.Unique(false)
	}
	rjaq.modifiers = append(rjaq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return rjaq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rjaq *ResumeJobApplicationQuery) Modify(modifiers ...func(s *sql.Selector)) *ResumeJobApplicationSelect {
	rjaq.modifiers = append(rjaq.modifiers, modifiers...)
	return rjaq.Select()
}

// ResumeJobApplicationGroupBy is the group-by builder for ResumeJobApplication entities.
type ResumeJobApplicationGroupBy struct {
	selector
	build *ResumeJobApplicationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rjagb *ResumeJobApplicationGroupBy) Aggregate(fns ...AggregateFunc) *ResumeJobApplicationGroupBy {
	rjagb.fns = append(rjagb.fns, fns...)
	return rjagb
}

// Scan applies the selector query and scans the result into the given value.
func (rjagb *ResumeJobApplicationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rjagb.build.ctx, ent.OpQueryGroupBy)
	if err := rjagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeJobApplicationQuery, *ResumeJobApplicationGroupBy](ctx, rjagb.build, rjagb, rjagb.build.inters, v)
}

func (rjagb *ResumeJobApplicationGroupBy) sqlScan(ctx context.Context, root *ResumeJobApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rjagb.fns))
	for _, fn := range rjagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rjagb.flds)+len(rjagb.fns))
		for _, f := range *rjagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rjagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rjagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResumeJobApplicationSelect is the builder for selecting fields of ResumeJobApplication entities.
type ResumeJobApplicationSelect struct {
	*ResumeJobApplicationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rjas *ResumeJobApplicationSelect) Aggregate(fns ...AggregateFunc) *ResumeJobApplicationSelect {
	rjas.fns = append(rjas.fns, fns...)
	return rjas
}

// Scan applies the selector query and scans the result into the given value.
func (rjas *ResumeJobApplicationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rjas.ctx, ent.OpQuerySelect)
	if err := rjas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeJobApplicationQuery, *ResumeJobApplicationSelect](ctx, rjas.ResumeJobApplicationQuery, rjas, rjas.inters, v)
}

func (rjas *ResumeJobApplicationSelect) sqlScan(ctx context.Context, root *ResumeJobApplicationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rjas.fns))
	for _, fn := range rjas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rjas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rjas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rjas *ResumeJobApplicationSelect) Modify(modifiers ...func(s *sql.Selector)) *ResumeJobApplicationSelect {
	rjas.modifiers = append(rjas.modifiers, modifiers...)
	return rjas
}
