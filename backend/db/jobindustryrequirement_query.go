// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/WhaleHire/backend/db/jobindustryrequirement"
	"github.com/chaitin/WhaleHire/backend/db/jobposition"
	"github.com/chaitin/WhaleHire/backend/db/predicate"
	"github.com/google/uuid"
)

// JobIndustryRequirementQuery is the builder for querying JobIndustryRequirement entities.
type JobIndustryRequirementQuery struct {
	config
	ctx        *QueryContext
	order      []jobindustryrequirement.OrderOption
	inters     []Interceptor
	predicates []predicate.JobIndustryRequirement
	withJob    *JobPositionQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobIndustryRequirementQuery builder.
func (jirq *JobIndustryRequirementQuery) Where(ps ...predicate.JobIndustryRequirement) *JobIndustryRequirementQuery {
	jirq.predicates = append(jirq.predicates, ps...)
	return jirq
}

// Limit the number of records to be returned by this query.
func (jirq *JobIndustryRequirementQuery) Limit(limit int) *JobIndustryRequirementQuery {
	jirq.ctx.Limit = &limit
	return jirq
}

// Offset to start from.
func (jirq *JobIndustryRequirementQuery) Offset(offset int) *JobIndustryRequirementQuery {
	jirq.ctx.Offset = &offset
	return jirq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jirq *JobIndustryRequirementQuery) Unique(unique bool) *JobIndustryRequirementQuery {
	jirq.ctx.Unique = &unique
	return jirq
}

// Order specifies how the records should be ordered.
func (jirq *JobIndustryRequirementQuery) Order(o ...jobindustryrequirement.OrderOption) *JobIndustryRequirementQuery {
	jirq.order = append(jirq.order, o...)
	return jirq
}

// QueryJob chains the current query on the "job" edge.
func (jirq *JobIndustryRequirementQuery) QueryJob() *JobPositionQuery {
	query := (&JobPositionClient{config: jirq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := jirq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := jirq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jobindustryrequirement.Table, jobindustryrequirement.FieldID, selector),
			sqlgraph.To(jobposition.Table, jobposition.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, jobindustryrequirement.JobTable, jobindustryrequirement.JobColumn),
		)
		fromU = sqlgraph.SetNeighbors(jirq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first JobIndustryRequirement entity from the query.
// Returns a *NotFoundError when no JobIndustryRequirement was found.
func (jirq *JobIndustryRequirementQuery) First(ctx context.Context) (*JobIndustryRequirement, error) {
	nodes, err := jirq.Limit(1).All(setContextOp(ctx, jirq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobindustryrequirement.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) FirstX(ctx context.Context) *JobIndustryRequirement {
	node, err := jirq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobIndustryRequirement ID from the query.
// Returns a *NotFoundError when no JobIndustryRequirement ID was found.
func (jirq *JobIndustryRequirementQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = jirq.Limit(1).IDs(setContextOp(ctx, jirq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobindustryrequirement.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := jirq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobIndustryRequirement entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobIndustryRequirement entity is found.
// Returns a *NotFoundError when no JobIndustryRequirement entities are found.
func (jirq *JobIndustryRequirementQuery) Only(ctx context.Context) (*JobIndustryRequirement, error) {
	nodes, err := jirq.Limit(2).All(setContextOp(ctx, jirq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobindustryrequirement.Label}
	default:
		return nil, &NotSingularError{jobindustryrequirement.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) OnlyX(ctx context.Context) *JobIndustryRequirement {
	node, err := jirq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobIndustryRequirement ID in the query.
// Returns a *NotSingularError when more than one JobIndustryRequirement ID is found.
// Returns a *NotFoundError when no entities are found.
func (jirq *JobIndustryRequirementQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = jirq.Limit(2).IDs(setContextOp(ctx, jirq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobindustryrequirement.Label}
	default:
		err = &NotSingularError{jobindustryrequirement.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := jirq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobIndustryRequirements.
func (jirq *JobIndustryRequirementQuery) All(ctx context.Context) ([]*JobIndustryRequirement, error) {
	ctx = setContextOp(ctx, jirq.ctx, ent.OpQueryAll)
	if err := jirq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JobIndustryRequirement, *JobIndustryRequirementQuery]()
	return withInterceptors[[]*JobIndustryRequirement](ctx, jirq, qr, jirq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) AllX(ctx context.Context) []*JobIndustryRequirement {
	nodes, err := jirq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobIndustryRequirement IDs.
func (jirq *JobIndustryRequirementQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if jirq.ctx.Unique == nil && jirq.path != nil {
		jirq.Unique(true)
	}
	ctx = setContextOp(ctx, jirq.ctx, ent.OpQueryIDs)
	if err = jirq.Select(jobindustryrequirement.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := jirq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jirq *JobIndustryRequirementQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jirq.ctx, ent.OpQueryCount)
	if err := jirq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jirq, querierCount[*JobIndustryRequirementQuery](), jirq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) CountX(ctx context.Context) int {
	count, err := jirq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jirq *JobIndustryRequirementQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jirq.ctx, ent.OpQueryExist)
	switch _, err := jirq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jirq *JobIndustryRequirementQuery) ExistX(ctx context.Context) bool {
	exist, err := jirq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobIndustryRequirementQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jirq *JobIndustryRequirementQuery) Clone() *JobIndustryRequirementQuery {
	if jirq == nil {
		return nil
	}
	return &JobIndustryRequirementQuery{
		config:     jirq.config,
		ctx:        jirq.ctx.Clone(),
		order:      append([]jobindustryrequirement.OrderOption{}, jirq.order...),
		inters:     append([]Interceptor{}, jirq.inters...),
		predicates: append([]predicate.JobIndustryRequirement{}, jirq.predicates...),
		withJob:    jirq.withJob.Clone(),
		// clone intermediate query.
		sql:       jirq.sql.Clone(),
		path:      jirq.path,
		modifiers: append([]func(*sql.Selector){}, jirq.modifiers...),
	}
}

// WithJob tells the query-builder to eager-load the nodes that are connected to
// the "job" edge. The optional arguments are used to configure the query builder of the edge.
func (jirq *JobIndustryRequirementQuery) WithJob(opts ...func(*JobPositionQuery)) *JobIndustryRequirementQuery {
	query := (&JobPositionClient{config: jirq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	jirq.withJob = query
	return jirq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobIndustryRequirement.Query().
//		GroupBy(jobindustryrequirement.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (jirq *JobIndustryRequirementQuery) GroupBy(field string, fields ...string) *JobIndustryRequirementGroupBy {
	jirq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JobIndustryRequirementGroupBy{build: jirq}
	grbuild.flds = &jirq.ctx.Fields
	grbuild.label = jobindustryrequirement.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.JobIndustryRequirement.Query().
//		Select(jobindustryrequirement.FieldDeletedAt).
//		Scan(ctx, &v)
func (jirq *JobIndustryRequirementQuery) Select(fields ...string) *JobIndustryRequirementSelect {
	jirq.ctx.Fields = append(jirq.ctx.Fields, fields...)
	sbuild := &JobIndustryRequirementSelect{JobIndustryRequirementQuery: jirq}
	sbuild.label = jobindustryrequirement.Label
	sbuild.flds, sbuild.scan = &jirq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JobIndustryRequirementSelect configured with the given aggregations.
func (jirq *JobIndustryRequirementQuery) Aggregate(fns ...AggregateFunc) *JobIndustryRequirementSelect {
	return jirq.Select().Aggregate(fns...)
}

func (jirq *JobIndustryRequirementQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jirq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jirq); err != nil {
				return err
			}
		}
	}
	for _, f := range jirq.ctx.Fields {
		if !jobindustryrequirement.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if jirq.path != nil {
		prev, err := jirq.path(ctx)
		if err != nil {
			return err
		}
		jirq.sql = prev
	}
	return nil
}

func (jirq *JobIndustryRequirementQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobIndustryRequirement, error) {
	var (
		nodes       = []*JobIndustryRequirement{}
		_spec       = jirq.querySpec()
		loadedTypes = [1]bool{
			jirq.withJob != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobIndustryRequirement).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobIndustryRequirement{config: jirq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(jirq.modifiers) > 0 {
		_spec.Modifiers = jirq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jirq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := jirq.withJob; query != nil {
		if err := jirq.loadJob(ctx, query, nodes, nil,
			func(n *JobIndustryRequirement, e *JobPosition) { n.Edges.Job = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (jirq *JobIndustryRequirementQuery) loadJob(ctx context.Context, query *JobPositionQuery, nodes []*JobIndustryRequirement, init func(*JobIndustryRequirement), assign func(*JobIndustryRequirement, *JobPosition)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*JobIndustryRequirement)
	for i := range nodes {
		fk := nodes[i].JobID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(jobposition.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "job_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (jirq *JobIndustryRequirementQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jirq.querySpec()
	if len(jirq.modifiers) > 0 {
		_spec.Modifiers = jirq.modifiers
	}
	_spec.Node.Columns = jirq.ctx.Fields
	if len(jirq.ctx.Fields) > 0 {
		_spec.Unique = jirq.ctx.Unique != nil && *jirq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jirq.driver, _spec)
}

func (jirq *JobIndustryRequirementQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jobindustryrequirement.Table, jobindustryrequirement.Columns, sqlgraph.NewFieldSpec(jobindustryrequirement.FieldID, field.TypeUUID))
	_spec.From = jirq.sql
	if unique := jirq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jirq.path != nil {
		_spec.Unique = true
	}
	if fields := jirq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobindustryrequirement.FieldID)
		for i := range fields {
			if fields[i] != jobindustryrequirement.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if jirq.withJob != nil {
			_spec.Node.AddColumnOnce(jobindustryrequirement.FieldJobID)
		}
	}
	if ps := jirq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jirq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jirq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jirq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jirq *JobIndustryRequirementQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jirq.driver.Dialect())
	t1 := builder.Table(jobindustryrequirement.Table)
	columns := jirq.ctx.Fields
	if len(columns) == 0 {
		columns = jobindustryrequirement.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jirq.sql != nil {
		selector = jirq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jirq.ctx.Unique != nil && *jirq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range jirq.modifiers {
		m(selector)
	}
	for _, p := range jirq.predicates {
		p(selector)
	}
	for _, p := range jirq.order {
		p(selector)
	}
	if offset := jirq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jirq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (jirq *JobIndustryRequirementQuery) ForUpdate(opts ...sql.LockOption) *JobIndustryRequirementQuery {
	if jirq.driver.Dialect() == dialect.Postgres {
		jirq.Unique(false)
	}
	jirq.modifiers = append(jirq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return jirq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (jirq *JobIndustryRequirementQuery) ForShare(opts ...sql.LockOption) *JobIndustryRequirementQuery {
	if jirq.driver.Dialect() == dialect.Postgres {
		jirq.Unique(false)
	}
	jirq.modifiers = append(jirq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return jirq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (jirq *JobIndustryRequirementQuery) Modify(modifiers ...func(s *sql.Selector)) *JobIndustryRequirementSelect {
	jirq.modifiers = append(jirq.modifiers, modifiers...)
	return jirq.Select()
}

// JobIndustryRequirementGroupBy is the group-by builder for JobIndustryRequirement entities.
type JobIndustryRequirementGroupBy struct {
	selector
	build *JobIndustryRequirementQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jirgb *JobIndustryRequirementGroupBy) Aggregate(fns ...AggregateFunc) *JobIndustryRequirementGroupBy {
	jirgb.fns = append(jirgb.fns, fns...)
	return jirgb
}

// Scan applies the selector query and scans the result into the given value.
func (jirgb *JobIndustryRequirementGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jirgb.build.ctx, ent.OpQueryGroupBy)
	if err := jirgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobIndustryRequirementQuery, *JobIndustryRequirementGroupBy](ctx, jirgb.build, jirgb, jirgb.build.inters, v)
}

func (jirgb *JobIndustryRequirementGroupBy) sqlScan(ctx context.Context, root *JobIndustryRequirementQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jirgb.fns))
	for _, fn := range jirgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jirgb.flds)+len(jirgb.fns))
		for _, f := range *jirgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jirgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jirgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JobIndustryRequirementSelect is the builder for selecting fields of JobIndustryRequirement entities.
type JobIndustryRequirementSelect struct {
	*JobIndustryRequirementQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jirs *JobIndustryRequirementSelect) Aggregate(fns ...AggregateFunc) *JobIndustryRequirementSelect {
	jirs.fns = append(jirs.fns, fns...)
	return jirs
}

// Scan applies the selector query and scans the result into the given value.
func (jirs *JobIndustryRequirementSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jirs.ctx, ent.OpQuerySelect)
	if err := jirs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobIndustryRequirementQuery, *JobIndustryRequirementSelect](ctx, jirs.JobIndustryRequirementQuery, jirs, jirs.inters, v)
}

func (jirs *JobIndustryRequirementSelect) sqlScan(ctx context.Context, root *JobIndustryRequirementQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jirs.fns))
	for _, fn := range jirs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jirs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jirs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (jirs *JobIndustryRequirementSelect) Modify(modifiers ...func(s *sql.Selector)) *JobIndustryRequirementSelect {
	jirs.modifiers = append(jirs.modifiers, modifiers...)
	return jirs
}
