// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/WhaleHire/backend/db/predicate"
	"github.com/chaitin/WhaleHire/backend/db/resume"
	"github.com/chaitin/WhaleHire/backend/db/resumeexperience"
	"github.com/google/uuid"
)

// ResumeExperienceQuery is the builder for querying ResumeExperience entities.
type ResumeExperienceQuery struct {
	config
	ctx        *QueryContext
	order      []resumeexperience.OrderOption
	inters     []Interceptor
	predicates []predicate.ResumeExperience
	withResume *ResumeQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResumeExperienceQuery builder.
func (req *ResumeExperienceQuery) Where(ps ...predicate.ResumeExperience) *ResumeExperienceQuery {
	req.predicates = append(req.predicates, ps...)
	return req
}

// Limit the number of records to be returned by this query.
func (req *ResumeExperienceQuery) Limit(limit int) *ResumeExperienceQuery {
	req.ctx.Limit = &limit
	return req
}

// Offset to start from.
func (req *ResumeExperienceQuery) Offset(offset int) *ResumeExperienceQuery {
	req.ctx.Offset = &offset
	return req
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (req *ResumeExperienceQuery) Unique(unique bool) *ResumeExperienceQuery {
	req.ctx.Unique = &unique
	return req
}

// Order specifies how the records should be ordered.
func (req *ResumeExperienceQuery) Order(o ...resumeexperience.OrderOption) *ResumeExperienceQuery {
	req.order = append(req.order, o...)
	return req
}

// QueryResume chains the current query on the "resume" edge.
func (req *ResumeExperienceQuery) QueryResume() *ResumeQuery {
	query := (&ResumeClient{config: req.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := req.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := req.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumeexperience.Table, resumeexperience.FieldID, selector),
			sqlgraph.To(resume.Table, resume.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, resumeexperience.ResumeTable, resumeexperience.ResumeColumn),
		)
		fromU = sqlgraph.SetNeighbors(req.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResumeExperience entity from the query.
// Returns a *NotFoundError when no ResumeExperience was found.
func (req *ResumeExperienceQuery) First(ctx context.Context) (*ResumeExperience, error) {
	nodes, err := req.Limit(1).All(setContextOp(ctx, req.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resumeexperience.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (req *ResumeExperienceQuery) FirstX(ctx context.Context) *ResumeExperience {
	node, err := req.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResumeExperience ID from the query.
// Returns a *NotFoundError when no ResumeExperience ID was found.
func (req *ResumeExperienceQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = req.Limit(1).IDs(setContextOp(ctx, req.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resumeexperience.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (req *ResumeExperienceQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := req.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResumeExperience entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResumeExperience entity is found.
// Returns a *NotFoundError when no ResumeExperience entities are found.
func (req *ResumeExperienceQuery) Only(ctx context.Context) (*ResumeExperience, error) {
	nodes, err := req.Limit(2).All(setContextOp(ctx, req.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resumeexperience.Label}
	default:
		return nil, &NotSingularError{resumeexperience.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (req *ResumeExperienceQuery) OnlyX(ctx context.Context) *ResumeExperience {
	node, err := req.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResumeExperience ID in the query.
// Returns a *NotSingularError when more than one ResumeExperience ID is found.
// Returns a *NotFoundError when no entities are found.
func (req *ResumeExperienceQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = req.Limit(2).IDs(setContextOp(ctx, req.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resumeexperience.Label}
	default:
		err = &NotSingularError{resumeexperience.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (req *ResumeExperienceQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := req.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResumeExperiences.
func (req *ResumeExperienceQuery) All(ctx context.Context) ([]*ResumeExperience, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryAll)
	if err := req.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResumeExperience, *ResumeExperienceQuery]()
	return withInterceptors[[]*ResumeExperience](ctx, req, qr, req.inters)
}

// AllX is like All, but panics if an error occurs.
func (req *ResumeExperienceQuery) AllX(ctx context.Context) []*ResumeExperience {
	nodes, err := req.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResumeExperience IDs.
func (req *ResumeExperienceQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if req.ctx.Unique == nil && req.path != nil {
		req.Unique(true)
	}
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryIDs)
	if err = req.Select(resumeexperience.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (req *ResumeExperienceQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := req.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (req *ResumeExperienceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryCount)
	if err := req.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, req, querierCount[*ResumeExperienceQuery](), req.inters)
}

// CountX is like Count, but panics if an error occurs.
func (req *ResumeExperienceQuery) CountX(ctx context.Context) int {
	count, err := req.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (req *ResumeExperienceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, req.ctx, ent.OpQueryExist)
	switch _, err := req.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (req *ResumeExperienceQuery) ExistX(ctx context.Context) bool {
	exist, err := req.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResumeExperienceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (req *ResumeExperienceQuery) Clone() *ResumeExperienceQuery {
	if req == nil {
		return nil
	}
	return &ResumeExperienceQuery{
		config:     req.config,
		ctx:        req.ctx.Clone(),
		order:      append([]resumeexperience.OrderOption{}, req.order...),
		inters:     append([]Interceptor{}, req.inters...),
		predicates: append([]predicate.ResumeExperience{}, req.predicates...),
		withResume: req.withResume.Clone(),
		// clone intermediate query.
		sql:       req.sql.Clone(),
		path:      req.path,
		modifiers: append([]func(*sql.Selector){}, req.modifiers...),
	}
}

// WithResume tells the query-builder to eager-load the nodes that are connected to
// the "resume" edge. The optional arguments are used to configure the query builder of the edge.
func (req *ResumeExperienceQuery) WithResume(opts ...func(*ResumeQuery)) *ResumeExperienceQuery {
	query := (&ResumeClient{config: req.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	req.withResume = query
	return req
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResumeExperience.Query().
//		GroupBy(resumeexperience.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (req *ResumeExperienceQuery) GroupBy(field string, fields ...string) *ResumeExperienceGroupBy {
	req.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResumeExperienceGroupBy{build: req}
	grbuild.flds = &req.ctx.Fields
	grbuild.label = resumeexperience.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.ResumeExperience.Query().
//		Select(resumeexperience.FieldDeletedAt).
//		Scan(ctx, &v)
func (req *ResumeExperienceQuery) Select(fields ...string) *ResumeExperienceSelect {
	req.ctx.Fields = append(req.ctx.Fields, fields...)
	sbuild := &ResumeExperienceSelect{ResumeExperienceQuery: req}
	sbuild.label = resumeexperience.Label
	sbuild.flds, sbuild.scan = &req.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResumeExperienceSelect configured with the given aggregations.
func (req *ResumeExperienceQuery) Aggregate(fns ...AggregateFunc) *ResumeExperienceSelect {
	return req.Select().Aggregate(fns...)
}

func (req *ResumeExperienceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range req.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, req); err != nil {
				return err
			}
		}
	}
	for _, f := range req.ctx.Fields {
		if !resumeexperience.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if req.path != nil {
		prev, err := req.path(ctx)
		if err != nil {
			return err
		}
		req.sql = prev
	}
	return nil
}

func (req *ResumeExperienceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResumeExperience, error) {
	var (
		nodes       = []*ResumeExperience{}
		_spec       = req.querySpec()
		loadedTypes = [1]bool{
			req.withResume != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResumeExperience).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResumeExperience{config: req.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(req.modifiers) > 0 {
		_spec.Modifiers = req.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, req.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := req.withResume; query != nil {
		if err := req.loadResume(ctx, query, nodes, nil,
			func(n *ResumeExperience, e *Resume) { n.Edges.Resume = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (req *ResumeExperienceQuery) loadResume(ctx context.Context, query *ResumeQuery, nodes []*ResumeExperience, init func(*ResumeExperience), assign func(*ResumeExperience, *Resume)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResumeExperience)
	for i := range nodes {
		fk := nodes[i].ResumeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(resume.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "resume_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (req *ResumeExperienceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := req.querySpec()
	if len(req.modifiers) > 0 {
		_spec.Modifiers = req.modifiers
	}
	_spec.Node.Columns = req.ctx.Fields
	if len(req.ctx.Fields) > 0 {
		_spec.Unique = req.ctx.Unique != nil && *req.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, req.driver, _spec)
}

func (req *ResumeExperienceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resumeexperience.Table, resumeexperience.Columns, sqlgraph.NewFieldSpec(resumeexperience.FieldID, field.TypeUUID))
	_spec.From = req.sql
	if unique := req.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if req.path != nil {
		_spec.Unique = true
	}
	if fields := req.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resumeexperience.FieldID)
		for i := range fields {
			if fields[i] != resumeexperience.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if req.withResume != nil {
			_spec.Node.AddColumnOnce(resumeexperience.FieldResumeID)
		}
	}
	if ps := req.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := req.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := req.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := req.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (req *ResumeExperienceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(req.driver.Dialect())
	t1 := builder.Table(resumeexperience.Table)
	columns := req.ctx.Fields
	if len(columns) == 0 {
		columns = resumeexperience.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if req.sql != nil {
		selector = req.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if req.ctx.Unique != nil && *req.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range req.modifiers {
		m(selector)
	}
	for _, p := range req.predicates {
		p(selector)
	}
	for _, p := range req.order {
		p(selector)
	}
	if offset := req.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := req.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (req *ResumeExperienceQuery) ForUpdate(opts ...sql.LockOption) *ResumeExperienceQuery {
	if req.driver.Dialect() == dialect.Postgres {
		req.Unique(false)
	}
	req.modifiers = append(req.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return req
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (req *ResumeExperienceQuery) ForShare(opts ...sql.LockOption) *ResumeExperienceQuery {
	if req.driver.Dialect() == dialect.Postgres {
		req.Unique(false)
	}
	req.modifiers = append(req.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return req
}

// Modify adds a query modifier for attaching custom logic to queries.
func (req *ResumeExperienceQuery) Modify(modifiers ...func(s *sql.Selector)) *ResumeExperienceSelect {
	req.modifiers = append(req.modifiers, modifiers...)
	return req.Select()
}

// ResumeExperienceGroupBy is the group-by builder for ResumeExperience entities.
type ResumeExperienceGroupBy struct {
	selector
	build *ResumeExperienceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (regb *ResumeExperienceGroupBy) Aggregate(fns ...AggregateFunc) *ResumeExperienceGroupBy {
	regb.fns = append(regb.fns, fns...)
	return regb
}

// Scan applies the selector query and scans the result into the given value.
func (regb *ResumeExperienceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, regb.build.ctx, ent.OpQueryGroupBy)
	if err := regb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeExperienceQuery, *ResumeExperienceGroupBy](ctx, regb.build, regb, regb.build.inters, v)
}

func (regb *ResumeExperienceGroupBy) sqlScan(ctx context.Context, root *ResumeExperienceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(regb.fns))
	for _, fn := range regb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*regb.flds)+len(regb.fns))
		for _, f := range *regb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*regb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := regb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResumeExperienceSelect is the builder for selecting fields of ResumeExperience entities.
type ResumeExperienceSelect struct {
	*ResumeExperienceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (res *ResumeExperienceSelect) Aggregate(fns ...AggregateFunc) *ResumeExperienceSelect {
	res.fns = append(res.fns, fns...)
	return res
}

// Scan applies the selector query and scans the result into the given value.
func (res *ResumeExperienceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, res.ctx, ent.OpQuerySelect)
	if err := res.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeExperienceQuery, *ResumeExperienceSelect](ctx, res.ResumeExperienceQuery, res, res.inters, v)
}

func (res *ResumeExperienceSelect) sqlScan(ctx context.Context, root *ResumeExperienceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(res.fns))
	for _, fn := range res.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*res.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := res.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (res *ResumeExperienceSelect) Modify(modifiers ...func(s *sql.Selector)) *ResumeExperienceSelect {
	res.modifiers = append(res.modifiers, modifiers...)
	return res
}
