// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/chaitin/WhaleHire/backend/db/predicate"
	"github.com/chaitin/WhaleHire/backend/db/resumemailboxcursor"
	"github.com/chaitin/WhaleHire/backend/db/resumemailboxsetting"
	"github.com/chaitin/WhaleHire/backend/db/resumemailboxstatistic"
	"github.com/chaitin/WhaleHire/backend/db/user"
	"github.com/google/uuid"
)

// ResumeMailboxSettingQuery is the builder for querying ResumeMailboxSetting entities.
type ResumeMailboxSettingQuery struct {
	config
	ctx            *QueryContext
	order          []resumemailboxsetting.OrderOption
	inters         []Interceptor
	predicates     []predicate.ResumeMailboxSetting
	withUploader   *UserQuery
	withCursors    *ResumeMailboxCursorQuery
	withStatistics *ResumeMailboxStatisticQuery
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ResumeMailboxSettingQuery builder.
func (rmsq *ResumeMailboxSettingQuery) Where(ps ...predicate.ResumeMailboxSetting) *ResumeMailboxSettingQuery {
	rmsq.predicates = append(rmsq.predicates, ps...)
	return rmsq
}

// Limit the number of records to be returned by this query.
func (rmsq *ResumeMailboxSettingQuery) Limit(limit int) *ResumeMailboxSettingQuery {
	rmsq.ctx.Limit = &limit
	return rmsq
}

// Offset to start from.
func (rmsq *ResumeMailboxSettingQuery) Offset(offset int) *ResumeMailboxSettingQuery {
	rmsq.ctx.Offset = &offset
	return rmsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rmsq *ResumeMailboxSettingQuery) Unique(unique bool) *ResumeMailboxSettingQuery {
	rmsq.ctx.Unique = &unique
	return rmsq
}

// Order specifies how the records should be ordered.
func (rmsq *ResumeMailboxSettingQuery) Order(o ...resumemailboxsetting.OrderOption) *ResumeMailboxSettingQuery {
	rmsq.order = append(rmsq.order, o...)
	return rmsq
}

// QueryUploader chains the current query on the "uploader" edge.
func (rmsq *ResumeMailboxSettingQuery) QueryUploader() *UserQuery {
	query := (&UserClient{config: rmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumemailboxsetting.Table, resumemailboxsetting.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, resumemailboxsetting.UploaderTable, resumemailboxsetting.UploaderColumn),
		)
		fromU = sqlgraph.SetNeighbors(rmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCursors chains the current query on the "cursors" edge.
func (rmsq *ResumeMailboxSettingQuery) QueryCursors() *ResumeMailboxCursorQuery {
	query := (&ResumeMailboxCursorClient{config: rmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumemailboxsetting.Table, resumemailboxsetting.FieldID, selector),
			sqlgraph.To(resumemailboxcursor.Table, resumemailboxcursor.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, resumemailboxsetting.CursorsTable, resumemailboxsetting.CursorsColumn),
		)
		fromU = sqlgraph.SetNeighbors(rmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStatistics chains the current query on the "statistics" edge.
func (rmsq *ResumeMailboxSettingQuery) QueryStatistics() *ResumeMailboxStatisticQuery {
	query := (&ResumeMailboxStatisticClient{config: rmsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := rmsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := rmsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(resumemailboxsetting.Table, resumemailboxsetting.FieldID, selector),
			sqlgraph.To(resumemailboxstatistic.Table, resumemailboxstatistic.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, resumemailboxsetting.StatisticsTable, resumemailboxsetting.StatisticsColumn),
		)
		fromU = sqlgraph.SetNeighbors(rmsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ResumeMailboxSetting entity from the query.
// Returns a *NotFoundError when no ResumeMailboxSetting was found.
func (rmsq *ResumeMailboxSettingQuery) First(ctx context.Context) (*ResumeMailboxSetting, error) {
	nodes, err := rmsq.Limit(1).All(setContextOp(ctx, rmsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{resumemailboxsetting.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) FirstX(ctx context.Context) *ResumeMailboxSetting {
	node, err := rmsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ResumeMailboxSetting ID from the query.
// Returns a *NotFoundError when no ResumeMailboxSetting ID was found.
func (rmsq *ResumeMailboxSettingQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rmsq.Limit(1).IDs(setContextOp(ctx, rmsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{resumemailboxsetting.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := rmsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ResumeMailboxSetting entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ResumeMailboxSetting entity is found.
// Returns a *NotFoundError when no ResumeMailboxSetting entities are found.
func (rmsq *ResumeMailboxSettingQuery) Only(ctx context.Context) (*ResumeMailboxSetting, error) {
	nodes, err := rmsq.Limit(2).All(setContextOp(ctx, rmsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{resumemailboxsetting.Label}
	default:
		return nil, &NotSingularError{resumemailboxsetting.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) OnlyX(ctx context.Context) *ResumeMailboxSetting {
	node, err := rmsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ResumeMailboxSetting ID in the query.
// Returns a *NotSingularError when more than one ResumeMailboxSetting ID is found.
// Returns a *NotFoundError when no entities are found.
func (rmsq *ResumeMailboxSettingQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = rmsq.Limit(2).IDs(setContextOp(ctx, rmsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{resumemailboxsetting.Label}
	default:
		err = &NotSingularError{resumemailboxsetting.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := rmsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ResumeMailboxSettings.
func (rmsq *ResumeMailboxSettingQuery) All(ctx context.Context) ([]*ResumeMailboxSetting, error) {
	ctx = setContextOp(ctx, rmsq.ctx, ent.OpQueryAll)
	if err := rmsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ResumeMailboxSetting, *ResumeMailboxSettingQuery]()
	return withInterceptors[[]*ResumeMailboxSetting](ctx, rmsq, qr, rmsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) AllX(ctx context.Context) []*ResumeMailboxSetting {
	nodes, err := rmsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ResumeMailboxSetting IDs.
func (rmsq *ResumeMailboxSettingQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if rmsq.ctx.Unique == nil && rmsq.path != nil {
		rmsq.Unique(true)
	}
	ctx = setContextOp(ctx, rmsq.ctx, ent.OpQueryIDs)
	if err = rmsq.Select(resumemailboxsetting.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := rmsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rmsq *ResumeMailboxSettingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rmsq.ctx, ent.OpQueryCount)
	if err := rmsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rmsq, querierCount[*ResumeMailboxSettingQuery](), rmsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) CountX(ctx context.Context) int {
	count, err := rmsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rmsq *ResumeMailboxSettingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rmsq.ctx, ent.OpQueryExist)
	switch _, err := rmsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rmsq *ResumeMailboxSettingQuery) ExistX(ctx context.Context) bool {
	exist, err := rmsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ResumeMailboxSettingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rmsq *ResumeMailboxSettingQuery) Clone() *ResumeMailboxSettingQuery {
	if rmsq == nil {
		return nil
	}
	return &ResumeMailboxSettingQuery{
		config:         rmsq.config,
		ctx:            rmsq.ctx.Clone(),
		order:          append([]resumemailboxsetting.OrderOption{}, rmsq.order...),
		inters:         append([]Interceptor{}, rmsq.inters...),
		predicates:     append([]predicate.ResumeMailboxSetting{}, rmsq.predicates...),
		withUploader:   rmsq.withUploader.Clone(),
		withCursors:    rmsq.withCursors.Clone(),
		withStatistics: rmsq.withStatistics.Clone(),
		// clone intermediate query.
		sql:       rmsq.sql.Clone(),
		path:      rmsq.path,
		modifiers: append([]func(*sql.Selector){}, rmsq.modifiers...),
	}
}

// WithUploader tells the query-builder to eager-load the nodes that are connected to
// the "uploader" edge. The optional arguments are used to configure the query builder of the edge.
func (rmsq *ResumeMailboxSettingQuery) WithUploader(opts ...func(*UserQuery)) *ResumeMailboxSettingQuery {
	query := (&UserClient{config: rmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rmsq.withUploader = query
	return rmsq
}

// WithCursors tells the query-builder to eager-load the nodes that are connected to
// the "cursors" edge. The optional arguments are used to configure the query builder of the edge.
func (rmsq *ResumeMailboxSettingQuery) WithCursors(opts ...func(*ResumeMailboxCursorQuery)) *ResumeMailboxSettingQuery {
	query := (&ResumeMailboxCursorClient{config: rmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rmsq.withCursors = query
	return rmsq
}

// WithStatistics tells the query-builder to eager-load the nodes that are connected to
// the "statistics" edge. The optional arguments are used to configure the query builder of the edge.
func (rmsq *ResumeMailboxSettingQuery) WithStatistics(opts ...func(*ResumeMailboxStatisticQuery)) *ResumeMailboxSettingQuery {
	query := (&ResumeMailboxStatisticClient{config: rmsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	rmsq.withStatistics = query
	return rmsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ResumeMailboxSetting.Query().
//		GroupBy(resumemailboxsetting.FieldDeletedAt).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (rmsq *ResumeMailboxSettingQuery) GroupBy(field string, fields ...string) *ResumeMailboxSettingGroupBy {
	rmsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ResumeMailboxSettingGroupBy{build: rmsq}
	grbuild.flds = &rmsq.ctx.Fields
	grbuild.label = resumemailboxsetting.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deleted_at,omitempty"`
//	}
//
//	client.ResumeMailboxSetting.Query().
//		Select(resumemailboxsetting.FieldDeletedAt).
//		Scan(ctx, &v)
func (rmsq *ResumeMailboxSettingQuery) Select(fields ...string) *ResumeMailboxSettingSelect {
	rmsq.ctx.Fields = append(rmsq.ctx.Fields, fields...)
	sbuild := &ResumeMailboxSettingSelect{ResumeMailboxSettingQuery: rmsq}
	sbuild.label = resumemailboxsetting.Label
	sbuild.flds, sbuild.scan = &rmsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ResumeMailboxSettingSelect configured with the given aggregations.
func (rmsq *ResumeMailboxSettingQuery) Aggregate(fns ...AggregateFunc) *ResumeMailboxSettingSelect {
	return rmsq.Select().Aggregate(fns...)
}

func (rmsq *ResumeMailboxSettingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rmsq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rmsq); err != nil {
				return err
			}
		}
	}
	for _, f := range rmsq.ctx.Fields {
		if !resumemailboxsetting.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if rmsq.path != nil {
		prev, err := rmsq.path(ctx)
		if err != nil {
			return err
		}
		rmsq.sql = prev
	}
	return nil
}

func (rmsq *ResumeMailboxSettingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ResumeMailboxSetting, error) {
	var (
		nodes       = []*ResumeMailboxSetting{}
		_spec       = rmsq.querySpec()
		loadedTypes = [3]bool{
			rmsq.withUploader != nil,
			rmsq.withCursors != nil,
			rmsq.withStatistics != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ResumeMailboxSetting).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ResumeMailboxSetting{config: rmsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(rmsq.modifiers) > 0 {
		_spec.Modifiers = rmsq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rmsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := rmsq.withUploader; query != nil {
		if err := rmsq.loadUploader(ctx, query, nodes, nil,
			func(n *ResumeMailboxSetting, e *User) { n.Edges.Uploader = e }); err != nil {
			return nil, err
		}
	}
	if query := rmsq.withCursors; query != nil {
		if err := rmsq.loadCursors(ctx, query, nodes,
			func(n *ResumeMailboxSetting) { n.Edges.Cursors = []*ResumeMailboxCursor{} },
			func(n *ResumeMailboxSetting, e *ResumeMailboxCursor) { n.Edges.Cursors = append(n.Edges.Cursors, e) }); err != nil {
			return nil, err
		}
	}
	if query := rmsq.withStatistics; query != nil {
		if err := rmsq.loadStatistics(ctx, query, nodes,
			func(n *ResumeMailboxSetting) { n.Edges.Statistics = []*ResumeMailboxStatistic{} },
			func(n *ResumeMailboxSetting, e *ResumeMailboxStatistic) {
				n.Edges.Statistics = append(n.Edges.Statistics, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (rmsq *ResumeMailboxSettingQuery) loadUploader(ctx context.Context, query *UserQuery, nodes []*ResumeMailboxSetting, init func(*ResumeMailboxSetting), assign func(*ResumeMailboxSetting, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ResumeMailboxSetting)
	for i := range nodes {
		fk := nodes[i].UploaderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "uploader_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (rmsq *ResumeMailboxSettingQuery) loadCursors(ctx context.Context, query *ResumeMailboxCursorQuery, nodes []*ResumeMailboxSetting, init func(*ResumeMailboxSetting), assign func(*ResumeMailboxSetting, *ResumeMailboxCursor)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ResumeMailboxSetting)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(resumemailboxcursor.FieldMailboxID)
	}
	query.Where(predicate.ResumeMailboxCursor(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(resumemailboxsetting.CursorsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.MailboxID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mailbox_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (rmsq *ResumeMailboxSettingQuery) loadStatistics(ctx context.Context, query *ResumeMailboxStatisticQuery, nodes []*ResumeMailboxSetting, init func(*ResumeMailboxSetting), assign func(*ResumeMailboxSetting, *ResumeMailboxStatistic)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*ResumeMailboxSetting)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(resumemailboxstatistic.FieldMailboxID)
	}
	query.Where(predicate.ResumeMailboxStatistic(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(resumemailboxsetting.StatisticsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.MailboxID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "mailbox_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (rmsq *ResumeMailboxSettingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rmsq.querySpec()
	if len(rmsq.modifiers) > 0 {
		_spec.Modifiers = rmsq.modifiers
	}
	_spec.Node.Columns = rmsq.ctx.Fields
	if len(rmsq.ctx.Fields) > 0 {
		_spec.Unique = rmsq.ctx.Unique != nil && *rmsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rmsq.driver, _spec)
}

func (rmsq *ResumeMailboxSettingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(resumemailboxsetting.Table, resumemailboxsetting.Columns, sqlgraph.NewFieldSpec(resumemailboxsetting.FieldID, field.TypeUUID))
	_spec.From = rmsq.sql
	if unique := rmsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rmsq.path != nil {
		_spec.Unique = true
	}
	if fields := rmsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, resumemailboxsetting.FieldID)
		for i := range fields {
			if fields[i] != resumemailboxsetting.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if rmsq.withUploader != nil {
			_spec.Node.AddColumnOnce(resumemailboxsetting.FieldUploaderID)
		}
	}
	if ps := rmsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rmsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rmsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rmsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rmsq *ResumeMailboxSettingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rmsq.driver.Dialect())
	t1 := builder.Table(resumemailboxsetting.Table)
	columns := rmsq.ctx.Fields
	if len(columns) == 0 {
		columns = resumemailboxsetting.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rmsq.sql != nil {
		selector = rmsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rmsq.ctx.Unique != nil && *rmsq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rmsq.modifiers {
		m(selector)
	}
	for _, p := range rmsq.predicates {
		p(selector)
	}
	for _, p := range rmsq.order {
		p(selector)
	}
	if offset := rmsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rmsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (rmsq *ResumeMailboxSettingQuery) ForUpdate(opts ...sql.LockOption) *ResumeMailboxSettingQuery {
	if rmsq.driver.Dialect() == dialect.Postgres {
		rmsq.Unique(false)
	}
	rmsq.modifiers = append(rmsq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return rmsq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (rmsq *ResumeMailboxSettingQuery) ForShare(opts ...sql.LockOption) *ResumeMailboxSettingQuery {
	if rmsq.driver.Dialect() == dialect.Postgres {
		rmsq.Unique(false)
	}
	rmsq.modifiers = append(rmsq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return rmsq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rmsq *ResumeMailboxSettingQuery) Modify(modifiers ...func(s *sql.Selector)) *ResumeMailboxSettingSelect {
	rmsq.modifiers = append(rmsq.modifiers, modifiers...)
	return rmsq.Select()
}

// ResumeMailboxSettingGroupBy is the group-by builder for ResumeMailboxSetting entities.
type ResumeMailboxSettingGroupBy struct {
	selector
	build *ResumeMailboxSettingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rmsgb *ResumeMailboxSettingGroupBy) Aggregate(fns ...AggregateFunc) *ResumeMailboxSettingGroupBy {
	rmsgb.fns = append(rmsgb.fns, fns...)
	return rmsgb
}

// Scan applies the selector query and scans the result into the given value.
func (rmsgb *ResumeMailboxSettingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rmsgb.build.ctx, ent.OpQueryGroupBy)
	if err := rmsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeMailboxSettingQuery, *ResumeMailboxSettingGroupBy](ctx, rmsgb.build, rmsgb, rmsgb.build.inters, v)
}

func (rmsgb *ResumeMailboxSettingGroupBy) sqlScan(ctx context.Context, root *ResumeMailboxSettingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rmsgb.fns))
	for _, fn := range rmsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rmsgb.flds)+len(rmsgb.fns))
		for _, f := range *rmsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rmsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rmsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ResumeMailboxSettingSelect is the builder for selecting fields of ResumeMailboxSetting entities.
type ResumeMailboxSettingSelect struct {
	*ResumeMailboxSettingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rmss *ResumeMailboxSettingSelect) Aggregate(fns ...AggregateFunc) *ResumeMailboxSettingSelect {
	rmss.fns = append(rmss.fns, fns...)
	return rmss
}

// Scan applies the selector query and scans the result into the given value.
func (rmss *ResumeMailboxSettingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rmss.ctx, ent.OpQuerySelect)
	if err := rmss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ResumeMailboxSettingQuery, *ResumeMailboxSettingSelect](ctx, rmss.ResumeMailboxSettingQuery, rmss, rmss.inters, v)
}

func (rmss *ResumeMailboxSettingSelect) sqlScan(ctx context.Context, root *ResumeMailboxSettingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rmss.fns))
	for _, fn := range rmss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rmss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rmss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rmss *ResumeMailboxSettingSelect) Modify(modifiers ...func(s *sql.Selector)) *ResumeMailboxSettingSelect {
	rmss.modifiers = append(rmss.modifiers, modifiers...)
	return rmss
}
